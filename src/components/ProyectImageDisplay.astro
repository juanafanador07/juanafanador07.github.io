---
import type { ImageMetadata } from "astro";
import { Picture } from "astro:assets";
import CardSpotlight from "./CardSpotlight.astro";
import CardTilt from "./CardTilt.astro";

interface Props {
  image: ImageMetadata;
  imageAlt: string;
}

const { image, imageAlt } = Astro.props;
---

<CardTilt>
  <div
    class="rounded-lg overflow-hidden shadow-xl transition-all"
    data-proyect-picture
  >
    <CardSpotlight>
      <div
        class="flex items-start p-2 gap-1 bg-gradient-to-t from-neutral-100 to-neutral-200"
      >
        <div class="size-2 rounded-full bg-red-500"></div>
        <div class="size-2 rounded-full bg-yellow-500"></div>
        <div class="size-2 rounded-full bg-green-500"></div>
      </div>
      <Picture
        densities={[1.5, 2, 3, 4, 5]}
        src={image}
        alt={imageAlt}
        formats={["avif", "webp"]}
      />
    </CardSpotlight>
  </div>
</CardTilt>
